# Firebase Configuration
# Get these from: https://console.firebase.google.com/
# 1. Create a new project or select existing
# 2. Go to Project Settings > General > Your apps
# 3. Click "Add app" > Web (</>) 
# 4. Register app and copy the config values
VITE_FIREBASE_API_KEY=your-firebase-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Google Workspace Integration
# Get these from: https://console.cloud.google.com/
# 1. Create new project or select existing
# 2. Enable APIs: Google Docs API, Google Drive API, Google Calendar API, Google Meet API
# 3. Go to "Credentials" > Create Credentials > OAuth 2.0 Client ID
# 4. Set authorized JavaScript origins: http://localhost:3000, https://yourdomain.com
# 5. Set authorized redirect URIs: http://localhost:3000/auth/google/callback
# 6. Copy Client ID and API Key
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=your-google-api-key

# Slack Integration
# Get these from: https://api.slack.com/apps
# 1. Click "Create New App" > From scratch
# 2. Add Bot Token Scopes: chat:write, channels:read, channels:write
# 3. Install app to workspace and copy Bot User OAuth Token
# 4. Under "OAuth & Permissions", add redirect URL: http://localhost:3000/auth/slack/callback
# 5. Copy Client ID and Client Secret
VITE_SLACK_CLIENT_ID=your-slack-client-id
VITE_SLACK_CLIENT_SECRET=your-slack-client-secret
VITE_SLACK_BOT_TOKEN=xoxb-your-bot-token

# Zoom Integration
# Get these from: https://marketplace.zoom.us/develop/create
# 1. Create "Server-to-Server OAuth" app
# 2. Add scopes: meeting:write, meeting:read
# 3. Copy Account ID, Client ID, Client Secret
VITE_ZOOM_ACCOUNT_ID=your-zoom-account-id
VITE_ZOOM_CLIENT_ID=your-zoom-client-id
VITE_ZOOM_CLIENT_SECRET=your-zoom-client-secret

# Microsoft Teams Integration
# Get these from: https://portal.azure.com/ > Azure Active Directory > App registrations
# 1. Click "New registration"
# 2. Set redirect URI: http://localhost:3000/auth/teams/callback
# 3. Go to "API permissions" > Add permission > Microsoft Graph
# 4. Add: OnlineMeetings.ReadWrite, Calendars.ReadWrite
# 5. Copy Application (client) ID and create Client Secret
VITE_TEAMS_CLIENT_ID=your-teams-client-id
VITE_TEAMS_CLIENT_SECRET=your-teams-client-secret
VITE_TEAMS_TENANT_ID=your-tenant-id
